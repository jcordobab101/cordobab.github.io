import ReactDOM from "react-dom/client";

import "./index.css"; // Default css
import "./boostrap5v/css/bootstrap.css" // Adding bootstrap library 
import App from "./App";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render( < App / > );
